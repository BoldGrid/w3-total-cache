; W3TC_QA_USER_INI
; W3TC QA — relax PHP output buffering for shutdown/flush regressions.
; Loaded from the WordPress document root when user_ini is enabled (typical for php-fpm).
; See https://www.php.net/manual/en/configuration.file.per-user.php

output_buffering = Off
zlib.output_compression = Off
